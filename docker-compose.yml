services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    ports:
      - "8081:5000"
    environment:
      - SECRET_KEY=dev-secret-change-in-production
